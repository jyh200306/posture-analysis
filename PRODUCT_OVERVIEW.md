# 워크모션(WorkMotion) v1.0 — 제품 개요

> PO 전달용 문서 · 코드 기준(`prototype/`) 작성

---

## 1. 한 줄 요약

**스마트폰 사진 한 장으로 자세를 측정해 점수·리포트를 만들고, 맞춤 교정 운동을 처방한 뒤 매일 수행을 추적해 주는 모바일 웹 자세 코칭 앱.**

핵심 차별점은 **온디바이스 처리**다. 사진은 외부 서버로 전송되지 않고 기기 안에서만 분석되며, 모든 데이터는 브라우저에 로컬 저장된다.

---

## 2. 해결하려는 문제 / 타깃

- **문제**: 거북목·어깨 비대칭·골반 틀어짐 등 자세 문제는 본인이 알아차리기 어렵고, 병원·전문가 측정은 비용·접근성 부담이 크다.
- **타깃**: 책상 근무·스마트폰 사용이 많아 자세가 무너진 일반 사용자. 온보딩에서 **거북목 개선 / 체형 균형 / 바른 자세 습관** 중 목표를 선택한다.
- **포지셔닝**: 의료 진단이 아닌 **"회차 간 변화를 보는 참고 지표 + 셀프 교정 루틴"**. (결과 화면에 "AI 기반 참고 정보이며 의료적 진단이 아닙니다" 고지 상시 노출)

---

## 3. 핵심 사용자 여정 (5탭 + 측정/결과 오버레이)

| 단계 | 화면 | 사용자가 하는 일 |
|------|------|------------------|
| ① 온보딩 | Onboarding | 이름·목표 입력 (최초 1회) |
| ② 측정 | Analyze | 정면/측면 선택 → 카메라 타이머 촬영 또는 사진 업로드 |
| ③ 결과 | Result | 종합 점수·항목별 각도·자세 패턴 확인 → "맞춤 루틴 만들기" |
| ④ 리포트 | BodyReport | 종합 점수·체형 나이·골격 시각화·근육 불균형·미래 위험 예측 |
| ⑤ 코치 | Coach | 처방된 오늘의 루틴 수행·체크, 연속일(streak) 관리 |
| ⑥ 히스토리 | History | 회차별 기록, 주간 활동 추이 |
| 홈 | Home | 최근 측정 점수·추이 스파크라인, 오늘 루틴 진행률, 오늘의 팁 |

### 측정 플로우 상세
1. **방향 선택**: 정면(좌우 균형) 또는 측면(전후 정렬)
2. **입력**: 카메라 타이머 촬영(3/5/10초) 또는 갤러리 업로드(JPG·PNG, 최대 10MB)
3. **자동 품질 검사**: 밝기·해상도를 검사해 어둡거나 저해상도면 경고(차단은 아님)
4. **분석**: 관절점 검출 → 각도 측정 → 점수·패턴 산출 (최소 1.6초 노출로 진행 과정 표시)
5. **결과**: 사진 위에 골격선 오버레이 + 점수 + 항목별 피드백(낮은 점수 순 최대 3개)

---

## 4. 제품을 구성하는 4개 엔진

데이터는 `사진 → 자세감지 → 점수화 → 패턴분류 → 운동처방 → 리포트`의 순수 파이프라인으로 흐른다.

### ① 자세 감지 엔진 (`lib/pose.ts`)
- Google **MediaPipe BlazePose(Lite)** 모델을 브라우저(WASM)에서 구동
- 33개 랜드마크 → 앱이 쓰는 17개 관절점으로 변환
- **임상 보정**: BlazePose의 귀 좌표가 거북목 측정 기준점(tragus)보다 앞에 찍히는 문제를 눈-귀 벡터로 보정해 과대평가를 줄임
- 몸통(어깨·골반) 신뢰도가 낮으면 "감지 실패"로 처리

### ② 점수화 엔진 (`lib/scoring.ts`)
- 운동학의 **정렬선 개념**: 측면은 귀·어깨·골반·발목이 한 수직선, 정면은 어깨·골반이 수평선을 이뤄야 정상
- 각 지표를 그 기준선에서 벗어난 **각도(°)**로 측정 → **사진 크기·거리와 무관해 회차 간 비교 가능**
- 정면 4지표(어깨 수평·골반 수평·머리 기울기·몸통 중심) / 측면 4지표(목 전방·상체 굽음·몸통 기울기·골반 정렬)
- 각도를 양호/주의/교정필요 3구간으로 점수화 후 가중 평균 → 종합 점수(0~100)

### ③ 패턴 분류 (`classifyPattern`)
- 지표 조합으로 대표 패턴 명명: 예) "전방머리자세(거북목) 패턴", "거북목·굽은 등 복합 패턴", "골반 비대칭 패턴" 등

### ④ 운동 처방 엔진 (`lib/routine.ts` + `lib/exercises.ts`)
- 점수가 낮은(양호 아님) 지표 **상위 2개**를 골라 지표별 프로그램에서 **최대 5개** 운동을 교차 선택
- 모든 지표 양호 시 → 유지용 기본 루틴
- **운동 DB**: 5개 카테고리(거북목/어깨/등/골반/체형) **총 27개 동작**, 각 동작에 수행 단계·호흡 큐·주의사항 포함
- 코치 화면에서 오늘 수행 체크 → 연속 완료일(streak), 주간 활동일 자동 집계

---

## 5. 리포트 (BodyReport)의 동작 방식 — PO가 꼭 알아야 할 부분

리포트는 정면·측면 **최신 기록을 합쳐** 생성하며, **런타임 LLM을 쓰지 않는다.** 모든 문장은 정적 템플릿에 측정값만 끼워 넣는 **룰 기반**이다(비용·지연·프라이버시 이점).

구성 요소:
- **종합 점수 + 등급**
- **체형 나이**: 실제 나이 대비 추정값 (점수가 낮을수록 높게 추정 — *실측이 아닌 추정 공식*)
- **골격 시각화**: 실제 촬영 키포인트로 2D 오버레이 선 렌더
- **근육 장력 불균형**: 패턴별 단축↔약화 근육 매핑
- **미래 위험 예측**: 방치 시 현재/1년/3년 위험을 타임라인으로 (교정 동기 부여)

---

## 6. 기술 스택 / 인프라

| 영역 | 선택 |
|------|------|
| 프레임워크 | React 19 + TypeScript |
| 빌드 | Vite 6 |
| 자세 AI | @mediapipe/tasks-vision (BlazePose Lite, 온디바이스 WASM) |
| 데이터 저장 | 브라우저 localStorage (서버 DB 없음) |
| 백엔드 | **없음** (모델·WASM만 CDN에서 1회 다운로드) |

**프라이버시 설계**: 원본 사진은 저장하지 않고, 리포트 렌더에 필요한 키포인트 좌표만 로컬 저장.

---

## 7. 현재 한계 / 의사결정 필요 사항 (PO 검토 포인트)

1. **점수 임계값이 미검증 초기 추정치**
   각 지표의 양호/교정 경계값과 가중치는 임상 검증 전 추정치다(코드 주석에 명시). → 실제 사진 데이터로 보정 필요. **신뢰도 확보가 제품 핵심 리스크.**

2. **방향(좌/우, 전/후)을 구분하지 않음**
   현재 엔진은 틀어진 각도의 *크기*만 잰다. 그래서 리포트의 어깨 비대칭·골반 경사는 "균형/중립"으로 고정되고 심각도만 표현된다. → 어느 쪽으로 틀어졌는지 알려주려면 엔진 확장 필요.

3. **체형 나이·근육·위험 예측은 추정 콘텐츠**
   실측 데이터가 아니라 점수·패턴에서 룰 기반으로 산출. → 마케팅·신뢰성 측면에서 표현 수위 정렬 필요.

4. **데이터가 기기에 종속(localStorage)**
   계정·클라우드 동기화가 없어 기기 변경·브라우저 초기화 시 기록이 사라짐. → 계정/백엔드 도입 여부는 로드맵 의사결정 사항.

5. **저장 한도**: 측정 기록 60개, 수행 로그 120일까지만 보관.

---

## 8. 제품 상태

- 버전: **v1.0** (자세 측정·코칭 앱으로 전면 개편 완료)
- 형태: 모바일 웹 프로토타입 (`prototype/`)
- 동작 범위: 측정→점수→리포트→처방→수행추적 **전 여정이 엔드투엔드로 동작**
